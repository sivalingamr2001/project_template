analyze the code base ensure all are working properly 

User can login and find exsisting budget record or create new budget 
Need to Search working properly
And sidebar woring as expected and active item highlight by primary color
Dashboard screen create button after enter details navigate to plan entry page do not close modal only 
Actual amount also editable 
Check create, update, get api are working properly 
Add backend as oracle table also need to working without error using the ConnectionDll.dll 
Persist form drafts in localStorage using a debounced save on input changes to prevent data loss on refresh or crashes.
Restore the draft on component mount and optionally prompt the user to resume editing.
Namespace keys per user and feature (e.g., draft:profile:userId) and include timestamps for expiry handling.
Clear the stored draft once the user successfully submits or discards the form. and show draft content in draft budget modal

ensure these all are workin as perfect